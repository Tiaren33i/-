function max(z,x){
    return(Math.pow(z,2)+Math.pow(x,2))
}
let j = max(3,5)
let v = max(3,7)

console.log(Math.max(j,v))
//22. Написать метод, вычисляющий значение x2 + y2. С его помощью определить, с какой парой чисел (a, b) или (с, d) значение будет максимальным